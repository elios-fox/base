import 'package:flutter/material.dart';

class TeamCreatePage extends StatelessWidget {
  const TeamCreatePage({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Team toevoegen')), body: const Center(child: Text('Team toevoegen — Fase 2')));
}
