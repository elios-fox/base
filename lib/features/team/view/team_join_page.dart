import 'package:flutter/material.dart';

class TeamJoinPage extends StatelessWidget {
  const TeamJoinPage({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Team joinen')), body: const Center(child: Text('Team joinen — Fase 2')));
}
